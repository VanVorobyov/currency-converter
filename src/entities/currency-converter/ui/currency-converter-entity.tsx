import { FC } from 'react';
import styles from './styles.module.scss';

export const CurrencyConverterEntity: FC = () => {
	return (
		<>
			<div className={styles.container}>CurrencyConverterEntity</div>
		</>
	);
};
